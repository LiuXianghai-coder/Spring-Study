server:
  port: 8080

spring:
  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"
  datasource:
    hikari:
      connectionTimeout: 30000
      idleTimeout: 600000
      maxLifetime: 1800000
      maxPoolSize: 16
    url: jdbc:mysql://127.0.0.1:3306/lxh_db
    username: root
    password: 12345678
  thymeleaf:
    cache: false
    mode: LEGACYHTML5

user:
  name: xhliu
  age: 22
  plugin: com.example.demo.plugin.BackupInfoPlugin

mybatis:
  configuration:
    cache-enabled: true
    local-cache-scope: statement
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    reflectorFactory: com.example.demo.reflect.TaskInfoReflectorFactory
    # reflectorFactory: taskInfoReflectorFactory
  mapperLocations: mybatis/mapper/*
